-- One row per customer account and score date with component health scores.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as customer_health_snapshot_id,
    cast(null as date   ) as score_date,
    cast(null as varchar) as customer_id,
    cast(null as double ) as health_score,
    cast(null as varchar) as account_name,
    cast(null as varchar) as segment,
    cast(null as varchar) as plan_tier,
    cast(null as varchar) as cohort_month,
    cast(null as varchar) as account_id,
    cast(null as double ) as usage_score,
    cast(null as double ) as nps_score,
    cast(null as double ) as support_score,
    cast(null as double ) as engagement_score
where false
