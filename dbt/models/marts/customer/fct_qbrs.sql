-- One row per scheduled quarterly business review.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as qbr_id,
    cast(null as date   ) as scheduled_date,
    cast(null as varchar) as csm,
    cast(null as double ) as completed_qbrs,
    cast(null as double ) as scheduled_qbrs,
    cast(null as boolean) as is_completed
where false
