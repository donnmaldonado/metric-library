-- One row per company and valuation date (market cap, net debt, EBITDA multiples).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as valuation_snapshot_id,
    cast(null as date   ) as as_of_date,
    cast(null as double ) as market_cap
where false
