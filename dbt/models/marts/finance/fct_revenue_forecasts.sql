-- One row per segment and forecast period with forecast and actual revenue.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as revenue_forecast_id,
    cast(null as date   ) as forecast_period_start,
    cast(null as varchar) as segment,
    cast(null as double ) as actual_revenue,
    cast(null as double ) as forecast_revenue
where false
