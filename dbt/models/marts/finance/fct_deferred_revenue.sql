-- One row per company, product tier and date with the deferred revenue balance.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as deferred_revenue_snapshot_id,
    cast(null as date   ) as as_of_date,
    cast(null as varchar) as company_id,
    cast(null as varchar) as product_tier,
    cast(null as double ) as deferred_amount
where false
