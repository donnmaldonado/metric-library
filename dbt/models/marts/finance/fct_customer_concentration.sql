-- One row per company and period with top-customer and total revenue/ARR.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as concentration_period_id,
    cast(null as date   ) as period_start,
    cast(null as varchar) as company_id,
    cast(null as double ) as top10_customer_revenue,
    cast(null as double ) as total_customer_revenue,
    cast(null as double ) as top10_arr,
    cast(null as double ) as total_arr
where false
