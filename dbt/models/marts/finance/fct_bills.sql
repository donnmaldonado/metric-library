-- One row per vendor bill (accounts payable).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as bill_id,
    cast(null as date   ) as bill_date,
    cast(null as varchar) as vendor
where false
