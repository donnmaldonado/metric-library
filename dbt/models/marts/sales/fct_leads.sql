-- One row per lead, with lifecycle stage and funnel flags.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as lead_id,
    cast(null as date   ) as created_date,
    cast(null as varchar) as campaign_id,
    cast(null as varchar) as rep_id,
    cast(null as varchar) as channel,
    cast(null as varchar) as campaign_name,
    cast(null as varchar) as lifecycle_stage,
    cast(null as varchar) as segment,
    cast(null as varchar) as stage,
    cast(null as varchar) as lead_source,
    cast(null as varchar) as source,
    cast(null as varchar) as lead_score,
    cast(null as varchar) as score,
    cast(null as double ) as total_leads,
    cast(null as date   ) as mql_date,
    cast(null as date   ) as sql_date
where false
