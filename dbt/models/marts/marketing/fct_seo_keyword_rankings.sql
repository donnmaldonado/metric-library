-- One row per tracked keyword and ranking date (snapshot).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as keyword_ranking_id,
    cast(null as date   ) as ranking_date,
    cast(null as varchar) as keyword_id,
    cast(null as double ) as ranking_position,
    cast(null as varchar) as keyword_group,
    cast(null as varchar) as page_name
where false
