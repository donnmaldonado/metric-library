-- One row per page view.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as page_view_id,
    cast(null as date   ) as view_date,
    cast(null as varchar) as page_id,
    cast(null as varchar) as user_id,
    cast(null as varchar) as referrer,
    cast(null as varchar) as device,
    cast(null as varchar) as page_name
where false
