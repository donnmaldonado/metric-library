-- One row per web form and day with views and submissions.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as form_day_id,
    cast(null as date   ) as event_date,
    cast(null as varchar) as form_name,
    cast(null as varchar) as page_name,
    cast(null as double ) as form_submissions,
    cast(null as double ) as form_views
where false
