-- One row per earned media mention.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as media_mention_id,
    cast(null as date   ) as mention_date,
    cast(null as varchar) as outlet,
    cast(null as double ) as estimated_media_value
where false
