-- One row per company and period with ARR and headcount for efficiency ratios.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as saas_efficiency_period_id,
    cast(null as date   ) as period_start
where false
