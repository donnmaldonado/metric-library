-- One row per team sprint.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as sprint_id,
    cast(null as date   ) as sprint_end_date,
    cast(null as varchar) as team,
    cast(null as double ) as story_points_completed
where false
