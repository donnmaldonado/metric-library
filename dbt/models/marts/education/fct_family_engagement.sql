-- One row per family per school year (attended at least one event or conference).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as family_engagement_record_id,
    cast(null as date   ) as school_year_start,
    cast(null as varchar) as family_id,
    cast(null as varchar) as school_id,
    cast(null as varchar) as school_year,
    cast(null as varchar) as school_name,
    cast(null as boolean) as attended_event
where false
