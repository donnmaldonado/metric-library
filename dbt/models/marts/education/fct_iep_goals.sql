-- One row per IEP goal per evaluation (mastered flag).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as iep_goal_id,
    cast(null as date   ) as goal_date,
    cast(null as varchar) as student_id,
    cast(null as varchar) as school_id,
    cast(null as varchar) as disability_category,
    cast(null as varchar) as grade_level,
    cast(null as boolean) as is_mastered
where false
