-- One row per student course enrollment (course level, completion).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as course_enrollment_id,
    cast(null as date   ) as term_start_date,
    cast(null as varchar) as student_id,
    cast(null as varchar) as school_id,
    cast(null as varchar) as course_id,
    cast(null as varchar) as grade_level,
    cast(null as varchar) as demographic_group,
    cast(null as varchar) as school_year,
    cast(null as varchar) as school_name,
    cast(null as varchar) as course_level,
    cast(null as boolean) as is_completed
where false
