-- One row per student discipline incident (type, consequence).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as discipline_incident_id,
    cast(null as date   ) as incident_date,
    cast(null as varchar) as student_id,
    cast(null as varchar) as school_id,
    cast(null as varchar) as incident_type,
    cast(null as varchar) as consequence,
    cast(null as varchar) as school_year,
    cast(null as varchar) as school_name,
    cast(null as varchar) as subgroup
where false
