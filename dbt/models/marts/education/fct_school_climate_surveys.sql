-- One row per school climate survey response.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as climate_survey_response_id,
    cast(null as date   ) as response_date,
    cast(null as varchar) as school_id,
    cast(null as varchar) as school_year,
    cast(null as varchar) as school_name,
    cast(null as varchar) as respondent_type,
    cast(null as varchar) as survey_cycle,
    cast(null as double ) as climate_score
where false
