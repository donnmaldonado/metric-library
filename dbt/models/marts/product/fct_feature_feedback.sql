-- One row per feature request or product feedback item.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as feedback_id,
    cast(null as date   ) as submitted_date,
    cast(null as varchar) as feature_area,
    cast(null as varchar) as segment,
    cast(null as varchar) as channel
where false
