-- One row per completed training (employee, hours).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as training_record_id,
    cast(null as date   ) as completed_date,
    cast(null as varchar) as employee_id,
    cast(null as varchar) as department,
    cast(null as varchar) as training_type,
    cast(null as double ) as training_hours
where false
