-- One row per employee separation (type, regrettable flag).
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as separation_id,
    cast(null as date   ) as separation_date,
    cast(null as varchar) as employee_id,
    cast(null as varchar) as department,
    cast(null as varchar) as job_level,
    cast(null as varchar) as location,
    cast(null as varchar) as separation_type,
    cast(null as boolean) as is_regrettable
where false
