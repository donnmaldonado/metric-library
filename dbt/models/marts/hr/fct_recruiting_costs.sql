-- One row per department and period with recruiting cost and hires made.
-- Stub: returns zero rows with the typed columns the metric library needs.
select
    cast(null as varchar) as recruiting_cost_period_id,
    cast(null as date   ) as period_start,
    cast(null as varchar) as department,
    cast(null as double ) as total_recruiting_cost,
    cast(null as double ) as hires_made
where false
