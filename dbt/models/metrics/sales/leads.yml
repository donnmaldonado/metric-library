metrics:
  - name: leads
    label: Leads
    description: Leads created in the period, from CRM and marketing automation
    type: simple
    type_params:
      measure: lead_count
    config:
      meta:
        metricId: leads
        tier: input
        domain: sales
        industry: cross_industry
