metrics:
  - name: stg_support_tickets
    label: Support Tickets
    description: Raw ticket count from helpdesk staging table
    type: derived
    type_params:
      expr: tickets_created
      metrics:
        - name: tickets_created
    config:
      meta:
        metricId: stg_support_tickets
        tier: input
        vertical: ops
        industry: cross_industry
