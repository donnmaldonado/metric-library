metrics:
  - name: stg_purchase_order_row
    label: Raw PO Row
    description: Single purchase order record with supplier, items, and receipt date.
    type: simple
    type_params:
      measure: po_amount
    config:
      meta:
        metricId: stg_purchase_order_row
        tier: input
        vertical: ops
        industry: manufacturing
