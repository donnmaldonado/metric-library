metrics:
  - name: stg_ticket_row
    label: Raw Ticket Row
    description: Single support ticket record with category, priority, and resolution data.
    type: derived
    type_params:
      expr: tickets_created
      metrics:
        - name: tickets_created
    config:
      meta:
        metricId: stg_ticket_row
        tier: input
        vertical: ops
        industry: cross_industry
