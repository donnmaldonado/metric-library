metrics:
  - name: stg_safety_incident_row
    label: Raw Safety Incident Row
    description: Single workplace safety incident record with type, severity, and department.
    type: simple
    type_params:
      measure: safety_incidents_incident_count
    config:
      meta:
        metricId: stg_safety_incident_row
        tier: input
        vertical: ops
        industry: manufacturing
