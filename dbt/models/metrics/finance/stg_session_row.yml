metrics:
  - name: stg_session_row
    label: Raw Session Row
    description: Single web session record from analytics platform.
    type: derived
    type_params:
      expr: website_sessions
      metrics:
        - name: website_sessions
    config:
      meta:
        metricId: stg_session_row
        tier: input
        vertical: finance
        industry: cross_industry
