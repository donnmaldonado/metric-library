metrics:
  - name: stg_events_count
    label: Events
    description: Raw event row count in the staging events table
    type: simple
    type_params:
      measure: user_events_event_count
    config:
      meta:
        metricId: stg_events_count
        tier: input
        vertical: finance
        industry: cross_industry
