metrics:
  - name: stg_employee_row
    label: Raw Employee Row
    description: Single employee record from HRIS with start date, department, and status.
    type: derived
    type_params:
      expr: stg_employees_count
      metrics:
        - name: stg_employees_count
    config:
      meta:
        metricId: stg_employee_row
        tier: input
        vertical: finance
        industry: cross_industry
