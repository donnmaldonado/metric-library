metrics:
  - name: stg_attendance_row
    label: Raw Attendance Row
    description: Single student daily attendance record — present, absent, or tardy.
    type: simple
    type_params:
      measure: record_count
    config:
      meta:
        metricId: stg_attendance_row
        tier: input
        vertical: finance
        industry: cross_industry
