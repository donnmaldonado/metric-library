metrics:
  - name: stg_survey_response_row
    label: Raw Survey Response
    description: Individual employee survey response record.
    type: simple
    type_params:
      measure: employee_survey_responses_avg_score
    config:
      meta:
        metricId: stg_survey_response_row
        tier: input
        vertical: finance
        industry: cross_industry
