metrics:
  - name: stg_employees_count
    label: Employees
    description: Raw employee row count in staging HR table
    type: simple
    type_params:
      measure: employee_snapshots_employee_count
    config:
      meta:
        metricId: stg_employees_count
        tier: input
        vertical: finance
        industry: cross_industry
