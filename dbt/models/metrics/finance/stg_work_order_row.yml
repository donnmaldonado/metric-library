metrics:
  - name: stg_work_order_row
    label: Raw Work Order Row
    description: Single maintenance or facilities work order record.
    type: simple
    type_params:
      measure: work_order_count
    config:
      meta:
        metricId: stg_work_order_row
        tier: input
        vertical: finance
        industry: cross_industry
