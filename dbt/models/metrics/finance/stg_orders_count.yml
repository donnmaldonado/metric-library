metrics:
  - name: stg_orders_count
    label: Orders
    description: Raw count of order records in the staging orders table
    type: simple
    type_params:
      measure: order_count
    config:
      meta:
        metricId: stg_orders_count
        tier: input
        vertical: finance
        industry: cross_industry
