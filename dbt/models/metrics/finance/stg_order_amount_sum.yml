metrics:
  - name: stg_order_amount_sum
    label: Order Amount
    description: Sum of order amounts in staging before deduplication
    type: simple
    type_params:
      measure: order_amount
    config:
      meta:
        metricId: stg_order_amount_sum
        tier: input
        vertical: finance
        industry: cross_industry
