metrics:
  - name: mttd
    label: MTTD
    description: Average hours from incident start to detection
    type: simple
    type_params:
      measure: mttd_value
    config:
      meta:
        metricId: mttd
        tier: kpi
        domain: engineering
        industry: cross_industry
