metrics:
  - name: stg_revenue_event_amount
    label: Raw Revenue Event
    description: Individual recognized revenue transaction row from billing system.
    type: derived
    type_params:
      expr: revenue
      metrics:
        - name: revenue
    config:
      meta:
        metricId: stg_revenue_event_amount
        tier: input
        vertical: pe
        industry: saas
