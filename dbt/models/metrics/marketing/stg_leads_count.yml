metrics:
  - name: stg_leads_count
    label: Leads
    description: Raw lead count from CRM staging table
    type: simple
    type_params:
      measure: lead_count
    config:
      meta:
        metricId: stg_leads_count
        tier: input
        vertical: marketing
        industry: cross_industry
