metrics:
  - name: stg_lead_row
    label: Raw Lead Row
    description: Single lead record from CRM or marketing automation with source and score.
    type: derived
    type_params:
      expr: stg_leads_count
      metrics:
        - name: stg_leads_count
    config:
      meta:
        metricId: stg_lead_row
        tier: input
        vertical: marketing
        industry: cross_industry
