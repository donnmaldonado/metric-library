metrics:
  - name: sql_count
    label: SQLs (sql_count)
    description: Leads accepted by sales as ready to work.
    type: derived
    type_params:
      expr: sql
      metrics:
        - name: sql
    config:
      meta:
        metricId: sql_count
        tier: kpi
        vertical: marketing
        industry: cross_industry
