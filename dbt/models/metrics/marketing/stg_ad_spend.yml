metrics:
  - name: stg_ad_spend
    label: Ad Spend
    description: Raw ad spend total from all connected ad platforms in staging
    type: derived
    type_params:
      expr: marketing_spend
      metrics:
        - name: marketing_spend
    config:
      meta:
        metricId: stg_ad_spend
        tier: input
        vertical: marketing
        industry: cross_industry
