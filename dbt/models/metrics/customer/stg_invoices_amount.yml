metrics:
  - name: stg_invoices_amount
    label: Invoice Amount
    description: Sum of all invoice amounts in staging
    type: derived
    type_params:
      expr: billings
      metrics:
        - name: billings
    config:
      meta:
        metricId: stg_invoices_amount
        tier: input
        vertical: customer
        industry: cross_industry
