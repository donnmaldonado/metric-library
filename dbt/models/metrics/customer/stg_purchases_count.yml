metrics:
  - name: stg_purchases_count
    label: Purchases
    description: Raw purchase transaction count in staging
    type: derived
    type_params:
      expr: stg_orders_count
      metrics:
        - name: stg_orders_count
    config:
      meta:
        metricId: stg_purchases_count
        tier: input
        vertical: customer
        industry: retail
