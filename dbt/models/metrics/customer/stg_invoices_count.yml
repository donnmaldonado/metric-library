metrics:
  - name: stg_invoices_count
    label: Invoices
    description: Raw invoice row count from billing system staging
    type: simple
    type_params:
      measure: invoice_count
    config:
      meta:
        metricId: stg_invoices_count
        tier: input
        vertical: customer
        industry: cross_industry
